package com.cjrcodes.ces.models;

public enum PhoneType {

	mobile,
	home,
	work
	
	/*
	 * //mobile, home, work, EMPTY
	 * 
	 * // Assigning a value to each enum private final String phoneType;
	 * PhoneType(String phoneType){ this.phoneType = phoneType; }
	 * 
	 * // Overriding toString() method to return "" instead of "EMPTY"
	 * 
	 * @Override public String toString(){ return this.phoneType; }
	 */
}
